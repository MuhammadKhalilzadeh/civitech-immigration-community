import { Stack } from "@mui/material";
import Sidebar from "../components/sidebar";

const Layout = () => {
  return (
    <Stack sx={{ padding: 2 }}>
      <Sidebar />
    </Stack>
  );
};

export default Layout;
